# Coding Conventions

The project has no implementation stack yet. Do not infer framework-specific
conventions until the first stack is selected.

When implementation starts, record naming, directory boundaries, state
management, testing patterns, data fetching, error handling, logging, and
framework rules here when they are not enforced automatically.

Repeatedly missed conventions should become more specific here and gain an
automated check where practical.
